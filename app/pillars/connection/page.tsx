import PillarPage from "../_components/pillar-page";

export default function ConnectionPillarPage() {
  return (
    <PillarPage
      title="Connection"
      intro="Strong relationships help people live longer."
      body="Shared meals create space for conversation, belonging, and trust. LIFE uses the table as a simple way to rebuild meaningful connection between friends, family, neighbors, and communities."
    />
  );
}
